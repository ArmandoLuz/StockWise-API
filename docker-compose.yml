services:
  api:
    build: .
    container_name: stockwise-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
